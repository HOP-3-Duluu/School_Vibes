import {Dimensions, SafeAreaView, StyleSheet, Text, View} from 'react-native';
import React from 'react';
import {Margin, Stack} from '../components';

export const Header = () => {
  return (
    <Stack direction="row" justifyContent="center">
      <Text>Header</Text>
    </Stack>
  );
};

const styles = StyleSheet.create({});
